category: security-misconfiguration
cwe: CWE-502
description: 检测不安全的反序列化：使用不安全的反序列化方法
id: unsafe-deserialization
language: all
name: Unsafe Deserialization
owasp: "A08:2021-Software and Data Integrity Failures"
remediation: "避免对不可信数据进行反序列化；使用安全的序列化格式（如 JSON）；对反序列化数据进行完整性校验和类型白名单过滤。"
references:
  - "https://owasp.org/www-community/vulnerabilities/Deserialization_of_untrusted_data"
  - "https://cwe.mitre.org/data/definitions/502.html"
patterns:
  - language: java
    pattern: (?i)(ObjectInputStream\s*\(|readObject\s*\(|readUnshared\s*\(|XMLDecoder\s*\(|XStream\s*\()
  - language: python
    pattern: (?i)(pickle\.loads?\s*\(|marshal\.loads?\s*\(|yaml\.load\s*\(|yaml\.unsafe_load\s*\()
  - language: php
    pattern: (?i)(unserialize\s*\()
severity: critical
