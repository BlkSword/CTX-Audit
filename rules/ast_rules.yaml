name: "AST Rules"
version: "1.0"
rules:
  - id: "no-console-log"
    name: "No Console Log"
    description: "Detects usage of console.log which should not be in production code."
    severity: "low"
    language: "typescript"
    query: |
      (call_expression
        function: (member_expression
          object: (identifier) @obj
          property: (property_identifier) @prop)
        (#eq? @obj "console")
        (#eq? @prop "log"))
    # 查询必须含字面量 "console" 才可能命中：不含则跳过该文件的 tree-sitter 解析
    prefilter: ["console"]
    cwe: "CWE-489"
